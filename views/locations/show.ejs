
<h2 class="eventByLocation">Events in <%= location.name %></h2>
<% if (eventsByLocation.length > 0) { %>
  <% eventsByLocation.forEach(event => { %>
    <img height="150" width="100" src="<%= event.coverImagePath %>">
  <% }) %>
<% } %>