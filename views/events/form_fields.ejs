<div>
  <label>Event</label>
  <input type="text" name="title" value="<%= event.title %>" />
</div>

<div>
  <label>Location</label>
  <select name="location">
    <% location.forEach(location =>{%> <% if(location.id===event.location){ %>
    <option
      selected
      label="<%= location.name %>"
      value="<%= location.id %>"
    ></option>
    <% } else { %>
    <option label="<%= location.name %>" value="<%=location.id %>"></option>
    <% } %> <% }) %>
  </select>
</div>

<div>
  <label>Price</label>
  <input type="number" name="cost" value="<% event.cost %>" />
</div>

<div>
  <label>Event Date</label>
  <input
    type="date"
    name="evenDate"
    value="<%= event.evenDate ==null ? '' :
    event.evenDate.toISOString().split('T')[0] %> "
  />
</div>

<div>
  <label>Cover</label>
  <input type="file" name="cover" class="filepond" />
</div>

<div>
  <label>Description</label>
  <textarea name="description"> <%=event.description %></textarea>
</div>
